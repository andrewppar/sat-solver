from setuptools import setup

setup(
   name='sat_solver',
   version='0.1.0',
   author='Andrew Parisi',
   author_email='andrew.p.parisi@gmail.com',
   packages=['sat_solver'],
   description='A sat solver written in python',
   long_description=open('README.rst').read(),
   install_requires=[],
   )
